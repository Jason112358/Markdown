# I know how to use Markdown

## #后要有空格才可以作标题  

另外标题还可以用=分隔显示（这是正文大小字体）  

这是一级标题
=
这是二级标题
-
两个空格以上回车就表示换行  
就像这样  
如果不是两个空格 或者不回车  就会默认还在一行而且preview中转化为一个空格

另外空行也可换行

但是会多一个空行

# 字体

*斜体文本* _斜体文本_

**粗体文本** __粗体文本__

***粗斜体文本*** ___粗斜体文本___

# 分隔线

可以用三个以上的
***
或者
___
甚至可以
* * *
还可以
*  *    *
他都会忽略空格的
_      _  _
网上说---也可以
---
但和他说的二级标题又矛盾了
***
这么看应该是二级标题
***
**需要注意的是**
***
要插入分隔线是不能在同一行键入其他字符的，否则会认为是要写的文本而非分隔线

# ~~删除线这么简单就不说了~~

<u>因为Markdown是将纯文本输入的文档转化为XHTML或HTML文档，所以可用html语言的一些语法及标签实现部分功能，比如这段我就用了\<u>来实现下划线的样式，挺好用的吧~</u>

# 脚注

好像不太需要用[^this]啊？
[^this]:以后迟早要用。

*这里出现了第一个问题：* **脚注失败！！**  
网上查了说是因为Markdown是没有统一规范的，不同编辑工具间可能会有不同的功能支持，说不定我这里的脚注在你那里就真的变成脚注了呢？

# 有序列表 与 无序列表 与 列表嵌套
## 有序列表
1. 第一项
2. 第二项
3. 第三项  
甚至可以  
2. 这是第四项他不会显示2
1. 他会自增的
0. 而且在这里好像发现了在刚输入完第一位后会将之后的字符自动转化为英文字符
0. 这可太方便了  
-1. 负数好像还不太支持自增
2. 不过也够用了

## 无序列表
* 第一项
* 第二项
* 第三项
+ 第四项
- 第五项

## 列表嵌套
5. 把两种列表嵌套在一起
    * 只要键入四个空格或一个Tab即可
4. 有序列表主要看第一次的数字，之后则会自动在其基础上自增
    * 这样有好处
    * 但肯定也有不好的地方
    + 他还会在列表嵌套的时候自动为你缩进至小列表内
    * 而且不分+还是*
        * 这与外部的列表有所区别
            + 如果第二层列表时就使用八个空格是不能产生嵌套列表的
                + 最多不知道能嵌套到哪，估计够用了
    + 而且这个小圈圈
        * 和这个小方方
            * 也只能用两层
                * 之后就只有小方方了

# 区块引用

> 用法是输入一个> 即可  
> 需要注意的是：>后面还有一个空格  
即 键入的是 **>+空格**
>> 它还可以实现区块引用的嵌套
>>> 就像这样  
>>> 如果输入等数量的>也不会有新的嵌套结构  
>> 如果输入更少的>就更不会有变化了  
但是怎么退出区块引用呢
< 这样也不行啊
>>> 
原来这样就能退出了
>> 
只要输入等量或更少的>序列加空格并且那一行没有其他输入即可
> 
正所谓 ***负负得正***
> 1. 提示：
> + 区块引用和各种列表都可套着用，还有字体之类的，*只要*能**想到**，~~做不到~~
>     + 第二层列表最好用四个空格而不是Tab
> 
+ 这里是列表里引用
    > 区块
    >> 这里的想法和列表嵌套是相似的
    > 
    二者非常相近

# 明天继续搞 今天就到这